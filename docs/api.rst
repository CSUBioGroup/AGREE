API Reference
=============

Core functions are exposed from the top-level package:

.. code-block:: python

   from AGREE import AGREEConfig, run_agree, run_deepwalk_ec

``run_agree`` is the primary entry point. ``run_deepwalk_ec`` is a compatibility
wrapper for earlier experiment notebooks.
