# Notebooks

This directory is reserved for lightweight tutorials and figure-oriented
analysis notebooks.

The cleaned Python package in `AGREE/` should be treated as the source of truth.
Notebook code should import package functions instead of duplicating model
implementation details.
